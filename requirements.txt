# Mission Analyzer -- залежності
#
# Стандартної бібліотеки Python (3.10+) вистачає для базового запуску
# (GUI, парсинг .waypoints, аналіз, графіки, карта місії, кеш тайлів).
# Пакети нижче потрібні для решти функцій -- без них програма все одно
# запуститься, просто відповідні кнопки/можливості будуть обмежені
# (детальніше -- ARCHITECTURE.md).

# Кнопка "Зберегти PDF" на сторінці "Аналіз"
reportlab>=4.0

# Список COM-портів і підключення до ArduPilot (панель під UA/EN на
# сторінці "Місія")
pyserial>=3.5

# Повноцінне MAVLink-з'єднання (Read/Write місії на борт, heartbeat)
pymavlink>=2.4

# JPEG-тайли карти (Google Satellite/Гібрид) і якісне масштабування
# логотипу
Pillow>=10.0
